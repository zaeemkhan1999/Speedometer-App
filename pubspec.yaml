name: com.zaeem.authapp.authapp
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_driver:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  sensors_plus: ^2.0.2
  neopop: ^1.0.2
  intl: ^0.18.0
  geolocator: ^9.0.2
  syncfusion_flutter_gauges: null
  permission_handler: ^10.2.0
  google_maps_flutter: ^2.2.5
  firebase_core: ^2.10.0
  firebase_auth: ^4.2.2
  cloud_firestore: ^4.5.2
  flutter_bloc: ^8.0.1
  shared_preferences: null
  lottie: ^1.3.0
flutter:
  uses-material-design: true
  assets:
    - assets/animations/
